# ============================================
# 洛瓦托水泵选型系统 - 环境变量配置
# ============================================
# ⚠️ 重要提示：
# 1. 请复制此文件为 .env 文件
# 2. 填写实际的配置值
# 3. 不要将 .env 文件提交到版本控制系统
# 4. 保护好敏感信息（密钥、密码等）
# ============================================

# ============================================
# JWT 认证配置
# ============================================
# JWT 签名密钥（必须）
# ⚠️ 生产环境请使用强随机密钥（至少 32 个字符）
# 生成命令: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# JWT 访问令牌有效期（秒）
JWT_ACCESS_TOKEN_EXPIRY=3600

# JWT 刷新令牌有效期（秒）
JWT_REFRESH_TOKEN_EXPIRY=604800

# ============================================
# 数据加密配置
# ============================================
# 数据加密密钥（必须）
# ⚠️ 生产环境请使用强随机密钥（至少 32 个字符）
# 生成命令: openssl rand -base64 32
ENCRYPTION_KEY=your-super-secret-encryption-key-minimum-32-characters-change-in-production

# 加密算法（默认: aes-256-gcm）
ENCRYPTION_ALGORITHM=aes-256-gcm

# ============================================
# 数据库配置
# ============================================
# PostgreSQL 数据库连接 URL（必须）
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://postgres:your-password@localhost:5432/lovato_pump

# 数据库主机（可选，如果使用 DATABASE_URL 则不需要）
DB_HOST=localhost

# 数据库端口（可选）
DB_PORT=5432

# 数据库名称（可选）
DB_NAME=lovato_pump

# 数据库用户名（可选）
DB_USER=postgres

# 数据库密码（可选）
DB_PASSWORD=your-password

# ============================================
# CORS 配置
# ============================================
# 允许的源（CORS 白名单）
# 多个域名用逗号分隔
# 格式: https://yourdomain.com,https://www.yourdomain.com,http://localhost:5000
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,http://localhost:5000,http://localhost:3000

# ============================================
# 对象存储配置（S3 兼容）
# ============================================
# 访问密钥（必须）
COZE_BUCKET_ACCESS_KEY=your_access_key_here

# 密钥（必须）
COZE_BUCKET_SECRET_KEY=your_secret_key_here

# 存储桶名称
COZE_BUCKET_NAME=your-bucket-name

# 存储桶区域
COZE_BUCKET_REGION=us-east-1

# 存储桶端点（可选，用于自建 S3）
# COZE_BUCKET_ENDPOINT=https://s3.your-domain.com

# ============================================
# 应用配置
# ============================================
# 应用名称
APP_NAME=洛瓦托水泵选型系统

# 应用环境（development, staging, production）
NODE_ENV=development

# 应用端口
PORT=5000

# 应用 URL
NEXT_PUBLIC_APP_URL=http://localhost:5000

# ============================================
# 微信小程序配置
# ============================================
# 微信小程序 AppID
WECHAT_MINIPROGRAM_APPID=your_wechat_appid

# 微信小程序 AppSecret
WECHAT_MINIPROGRAM_APPSECRET=your_wechat_appsecret

# ============================================
# Cloudflare Tunnel 配置
# ============================================
# Cloudflare Tunnel ID（可选）
CLOUDFLARE_TUNNEL_ID=your_tunnel_id

# Cloudflare Tunnel Token（可选）
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token

# ============================================
# 安全配置
# ============================================
# 是否启用速率限制
ENABLE_RATE_LIMIT=true

# 是否启用 HTTPS 强制
ENABLE_HTTPS_FORCE=false

# 是否启用 HSTS
ENABLE_HSTS=false

# ============================================
# 日志配置
# ============================================
# 日志级别（error, warn, info, debug）
LOG_LEVEL=info

# 是否启用详细日志
LOG_VERBOSE=false

# ============================================
# 其他配置
# ============================================
# 时区
TZ=Asia/Shanghai

# 语言
LOCALE=zh-CN

# ============================================
# 开发工具配置
# ============================================
# 是否启用开发工具
ENABLE_DEV_TOOLS=true

# 是否启用热重载
ENABLE_HOT_RELOAD=true
